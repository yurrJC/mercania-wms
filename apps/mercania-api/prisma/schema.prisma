// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum ItemStatus {
  INTAKE
  STORED
  LISTED
  RESERVED
  SOLD
  RETURNED
  DISCARDED
}

enum ListingStatus {
  ACTIVE
  SOLD
  EXPIRED
  REMOVED
}

model IsbnMaster {
  id         Int      @id @default(autoincrement())
  isbn       String   @unique @db.VarChar(13) // ISBN, UPC, or any product barcode
  title      String   @db.VarChar(500)
  author     String?  @db.VarChar(200) // Author for books, Director for DVDs, Artist for CDs
  publisher  String?  @db.VarChar(200) // Publisher for books, Studio for DVDs, Label for CDs
  pubYear    Int?
  binding    String?  @db.VarChar(50)  // Format: Hardcover, Paperback, DVD, CD, etc.
  imageUrl   String?
  categories String[]
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  items Item[]

  @@map("isbn_master")
}

model Item {
  id              Int       @id @default(autoincrement()) // Sequential: 1, 2, 3...
  isbn            String?   @db.VarChar(13) // ISBN, UPC, or product barcode
  conditionGrade  String?   @db.VarChar(10)
  conditionNotes  String?
  costCents       Int       @default(0)
  intakeDate      DateTime  @default(now())
  listedDate      DateTime? // Date when item was listed for sale
  soldDate        DateTime? // Date when item was sold
  soldYear        Int?      // Year when item was sold for aggregation
  soldMonth       Int?      // Month (1-12) when item was sold for aggregation
  priceCents      Int?      // Sale price in cents when sold
  currentStatus   ItemStatus @default(INTAKE)
  currentLocation String?   @db.VarChar(20)
  lotNumber       Int?      // Items in the same lot share the same lot number
  dvdMetadata     Json?     // Store DVD-specific metadata (season, region, videoFormat, etc.)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  isbnMaster        IsbnMaster?        @relation(fields: [isbn], references: [isbn])
  statusHistory     ItemStatusHistory[]
  listings          Listing[]
  orderLines        OrderLine[]
  cogsRecords       COGSRecord[]

  // Performance indexes for scale
  @@index([lotNumber])           // Fast lot queries
  @@index([isbn])                // Fast ISBN/barcode searches
  @@index([currentStatus])       // Fast status filtering
  @@index([currentLocation])     // Fast location filtering
  @@index([currentStatus, currentLocation]) // Combined filters
  @@index([createdAt])           // Fast ordering by creation date
  @@index([soldDate])            // Fast sales date filtering
  @@index([soldYear, soldMonth]) // Fast sales reporting aggregation
  @@map("items")
}

model ItemStatusHistory {
  id         Int       @id @default(autoincrement())
  itemId     Int       // Changed to Int to match Item.id
  fromStatus ItemStatus?
  toStatus   ItemStatus
  channel    String?   @db.VarChar(50)
  note       String?
  changedAt  DateTime  @default(now())

  item Item @relation(fields: [itemId], references: [id])

  @@map("item_status_history")
}

model Listing {
  id          Int           @id @default(autoincrement())
  itemId      Int           // Changed to Int to match Item.id
  channel     String        @db.VarChar(50)
  externalId  String?       @db.VarChar(100)
  priceCents  Int
  status      ListingStatus @default(ACTIVE)
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt

  item Item @relation(fields: [itemId], references: [id])

  @@map("listings")
}

model Order {
  id         Int          @id @default(autoincrement()) // Sequential for orders too
  channel    String       @db.VarChar(50)
  buyer      String?      @db.VarChar(200)
  totalCents Int
  createdAt  DateTime     @default(now())

  orderLines OrderLine[]

  @@map("orders")
}

model OrderLine {
  id         Int      @id @default(autoincrement())
  orderId    Int      // Changed to Int to match Order.id
  itemId     Int      // Changed to Int to match Item.id
  qty        Int      @default(1)
  saleCents  Int

  order Order @relation(fields: [orderId], references: [id])
  item  Item  @relation(fields: [itemId], references: [id])

  @@map("order_lines")
}

model COGRecord {
  id            Int      @id @default(autoincrement())
  recordDate    DateTime @default(now())
  startDate     DateTime
  endDate       DateTime
  totalAmount   Float
  itemsUpdated  Int
  averagePerItem Float
  createdAt     DateTime @default(now())
  
  @@map("cog_records")
}

model COGSRecord {
  id            Int      @id @default(autoincrement())
  itemId        Int      // The item that was sold
  costCents     Int      // Cost of the item when sold
  soldDate      DateTime // When the item was marked as sold
  soldMonth     Int      // Month (1-12) for aggregation
  soldYear      Int      // Year for aggregation
  financialYear Int      // Financial year (e.g., 2025 for Jul 2024 - Jun 2025)
  createdAt     DateTime @default(now())
  
  item Item @relation(fields: [itemId], references: [id])
  
  // Indexes for efficient reporting queries
  @@index([soldDate])
  @@index([soldYear, soldMonth])
  @@index([financialYear])
  @@index([itemId])
  
  @@map("cogs_records")
}